function  Calculate()
{
    var firstNumber= parseInt(document.getElementById("first").value);
    var secondNumber= parseInt(document.getElementById("second").value);
    var sum=first(firstNumber,secondNumber);
    var total=0;
    document.getElementById("result").value=sum;
}
function first(firstNumber,secondNumber)
{

    second(firstNumber,secondNumber);
    return total;
}
function second(firstNumber,secondNumber)
{
  sum(firstNumber,secondNumber)
  return total;
}
function sum(firstNumber,secondNumber)
{
  total=firstNumber+secondNumber;
  return total;
}
